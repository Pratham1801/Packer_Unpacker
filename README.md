# Packer_Unpacker
Packer-Unpacker in Java (CUI Application) A Java-based console application that allows users to pack multiple files into a single archive and unpack them back to their original form. The project demonstrates the use of File I/O streams, metadata handling, and command-line interaction without any GUI components.
